<!doctype html>
<html>
  <head>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="../../stylesheets/style.css">
  </head>

  <body>
    <div class="container-fluid">

      <div class="col-lg-8 col-md-8 col-sm-8 col-xs-8">
        <h1>What Are My Flatiron School <br />Friends Up To This Week?</h1>

        <% @events.each do |date, events| %>
          <h3><%= date.strftime("%A, %B %d, %Y") %></h3>
            <ul>
            <% events.each do |event| %>
              <p>&#9642; <a href="<%= event.url %>" target="_blank" style="color: rgb(<%= event.group.color %>); font-size: <%= event.font_size %>px; line-height: 1;"/>
                <%= event.name %></a>
              </p>
            <% end %>
            </ul>
        <% end %>

        <form action="/" method="GET">
          <button type="submit" class="btn btn-default btn-lg">Get more events</button>
        </form>
        <br />
        <p class="footer">Created by <a href="https://github.com/natalieparellano" target="_blank">Natalie Arellano</a> and <a href="http://www.thisisvanessa.com" target="_blank">Vanessa Dean</a> at the <a href="http://flatironschool.com/" target="_blank">Flatiron School</a>.</p>
      </div>

      <!--/////////// Group Key //////////-->
      <div id="key">

        <h2>Meetup Groups</h2>

        <% @groups.each do |group| %>
          <h4><a href="http://www.meetup.com/<%= group.urlname %>/" target="_blank" style="color: rgb(<%= group.color %>)">
            <%= group.name %></a>
          </h4>
        <% end %>

      </div>
      <!--/////////// End //////////-->

    </div>
  </body>
</html>

